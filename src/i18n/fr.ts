/**
 * French translation dictionary.
 * Starts as a copy of English — will be overridden by DB translations
 * or replaced once AI-generated translations are downloaded.
 */
import en from "./en";

const fr: Record<string, string> = { ...en };

export default fr;
