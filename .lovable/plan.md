

## Plan: Remove chapter prefixes from scenario titles

**`src/pages/Home.tsx`** — Update the three `title` fields in the `showcaseScenarios` array:

- `"Chapter 1 — Societas Templois"` → `"Societas Templois"`
- `"Chapter 5 — Flagellum Dei"` → `"Flagellum Dei"`
- `"Chapter 9 — The Mad King"` → `"The Mad King"`

Three string changes, no other modifications needed.

