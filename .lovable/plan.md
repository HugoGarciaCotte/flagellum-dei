

## Add Faith Explanation to Step 2

Small UI change in `CharacterCreationWizard.tsx` — add explanatory text to the Faith selection step (step 2) explaining that faith is a strong RP constraint but grants a one-time save.

### Change in `CharacterCreationWizard.tsx`

Find the step 2 (faith) rendering section and add a brief explanation paragraph before the feat list, something like:

> "Faith is a strong roleplay constraint — your character must act according to their beliefs. In exchange, faith can be invoked once to save your character from certain death."

